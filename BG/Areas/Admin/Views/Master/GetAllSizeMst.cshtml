@model List<BG_Application.CustomDTO.SizeViewModel>
@{
    ViewBag.Title = "Size Master";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}


<div class="main-content">
    <section class="section">
        <div class="section-body">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <h4>Size Master</h4>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-hover" id="size-master-table" style="width:100%;">
                                    <thead>
                                        <tr>
                                            <th>From Size</th>
                                            <th>To Size</th>
                                            <th>Alias Size</th>
                                            <th>Order By</th>
                                            <th>Status</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var C in Model)
                                        {
                                            <tr>
                                                <td>@C.FromSize</td>
                                                <td>@C.ToSize</td>
                                                <td>@C.SizeAlias</td>
                                                <td>@C.SortID</td>
                                                <td>
                                                    @{ if (C.Active == true)
                                                        {
                                                            <div class="badge badge-success badge-shadow">Active</div>
                                                        }
                                                        else
                                                        {
                                                            <div class="badge badge-danger badge-shadow">InActive</div>
                                                        }
                                                    }
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>